kind: pipeline
name: default

services:
- name: cors-anywhere
  image: node
  commands:
  - git clone https://github.com/Rob--W/cors-anywhere.git
  - cd cors-anywhere
  - npm install
  - export PORT=7000
  - node server.js

steps:
- name: script-builder
  image: node
  commands:
  - sleep 10
  - npm install
  - node web_fetch_build.js